{{title "LinkedIn-Registrations"}}
<h1>Registrations</h1>

<table class="table table-bordered table-striped">
    <thead>
      <tr>
          <th>E-mail</th>
          <th>User ID</th>
      </tr>
    </thead>
    <tbody>
    {{#each model as |invitation|}}
        <tr>
            <td>{{invitation.email}}</td>
            <th>{{invitation.id}}</th>
        </tr>
    {{/each}}

    </tbody>
</table>